import React from "react";
import ReactDOM from "react-dom/client";
import VamoosChat from "./VamoosChat";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VamoosChat />
  </React.StrictMode>
);
